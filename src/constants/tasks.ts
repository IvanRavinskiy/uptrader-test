export enum STATUS {
  QUEUE = 'Queue',
  DEVELOPMENT = 'Development',
  DONE = 'Done',
}

export enum PRIORITY {
  LOW = 'Low',
  MEDIUM = 'Medium',
  HIGH = 'High',
}

export enum ColumnIndex {
  QUEUE = '0',
  DEVELOPMENT = '1',
  DONE = '2',
}
